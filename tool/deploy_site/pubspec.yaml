name: elattar_deploy_site
description: >-
  Builds the deployable docs site: flutter build web with the injected site
  origin and base href, then stages the versioned registry into the output.
publish_to: none
version: 0.0.1

environment:
  sdk: ^3.12.2

dev_dependencies:
  test: ^1.25.8
