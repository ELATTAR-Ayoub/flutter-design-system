name: elattar_release_registry
description: Stages a generated registry into the published site artifact.
publish_to: none
version: 0.0.1

environment:
  sdk: ^3.12.2

dev_dependencies:
  test: ^1.25.8
