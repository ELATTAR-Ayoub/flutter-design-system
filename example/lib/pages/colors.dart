/// `/design-system/colors` — the Colors foundation page.
///
/// Stub: the real content (5 sections, 18 live-measured swatches, 4 Notes,
/// 2 Panels, foot-nav) lands in Task 9 and replaces the body of this file
/// only.
library;

import 'package:flutter/widgets.dart';

import 'placeholder.dart';

class ColorsPage extends StatelessWidget {
  const ColorsPage({super.key});

  @override
  Widget build(BuildContext context) =>
      const PlaceholderPage(eyebrow: 'Foundations', title: 'Colors');
}
