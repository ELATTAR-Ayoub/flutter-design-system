/// `/design-system` — the overview page (`app/design-system/page.tsx`).
///
/// Stub: the real content (header, `#foundations` index grid, `#components`
/// group cards, `#rules` list, closing value Note) lands in Task 8 and
/// replaces the body of this file only.
library;

import 'package:flutter/widgets.dart';

import 'placeholder.dart';

class OverviewPage extends StatelessWidget {
  const OverviewPage({super.key});

  @override
  Widget build(BuildContext context) => const PlaceholderPage(
        eyebrow: "Elattar's Design System",
        title: 'Design System',
      );
}
