/// `/design-system/spacing` — the Spacing & Layout foundation page.
///
/// Stub: the real content (10 scale bars, radius ladder, elevation set, grid
/// demo, breakpoint Meta, DoDont, foot-nav) lands in Task 11 and replaces the
/// body of this file only.
library;

import 'package:flutter/widgets.dart';

import 'placeholder.dart';

class SpacingPage extends StatelessWidget {
  const SpacingPage({super.key});

  @override
  Widget build(BuildContext context) =>
      const PlaceholderPage(eyebrow: 'Foundations', title: 'Spacing & Layout');
}
