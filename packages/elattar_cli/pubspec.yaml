name: elattar_cli
description: Source-first installer CLI for the Elattar Flutter design system.
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.12.2

dependencies:
  yaml: ^3.1.2

dev_dependencies:
  test: ^1.25.8

executables:
  elattar: elattar
